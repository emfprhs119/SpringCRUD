package com.min.mutabledata;

import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;

import static org.junit.jupiter.api.Assertions.*;

@SpringBootTest
class MutabledataApplicationTests {


	@Test
	void contextLoads() {

	}

	@Test
	void databaseAddTest(){
		/*
		assertTrue(userService.addUser(CrudUser.builder().name("하민수").email("emfprhs119@gmail.com").build()));
		assertFalse(userService.addUser("하민수", "emfprhs119@gmail.com"));
		assertTrue(userService.addUser("abc21","b1@g.com"));

		CrudUser m = userService.findById(2L).get();
		assertEquals(m.getEmail(),"b1@g.com");

		 */
	}
}
